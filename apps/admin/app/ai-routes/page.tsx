import { api } from '../proxy';import { PageHeader,SectionCard,Badge,EmptyState } from '../components/ui';import {JsonEditForm} from '../components/admin-client';
const fa:any={chat:'Chat / چت',image:'Image / عکس',video:'Video / ویدیو'};const modes=[{value:'auto',label:'Auto'},{value:'venice',label:'Force Venice'},{value:'openai-compatible',label:'Force OpenAI-compatible'},{value:'mock',label:'Mock only for dev'}];
export default async function Page(){const d=await api('/api/admin/ai-routes');const items=d.items||[];return <><PageHeader title="سیاست انتخاب مدل" desc="Auto برای چت از AI_PROVIDER_MODE محیط استفاده می‌کند؛ عکس/ویدیو اگر Provider واقعی ندارند غیرفعال نمایش داده می‌شوند."/><SectionCard title="وضعیت محیط"><p>AI_PROVIDER_MODE: <Badge tone="ok">{d.env?.mode||'venice'}</Badge></p><p>Venice: <Badge tone={d.env?.veniceConfigured?'ok':'failed'}>{d.env?.veniceConfigured?'تنظیم شده':'کلید وجود ندارد'}</Badge></p><p>OpenAI-compatible: <Badge tone={d.env?.openAICompatibleConfigured?'ok':'failed'}>{d.env?.openAICompatibleConfigured?'تنظیم شده':'تنظیم نشده'}</Badge></p></SectionCard><div className="grid">{items.map((x:any)=><SectionCard key={x.taskType} title={fa[x.taskType]||x.taskType}><p>وضعیت فعلی: <Badge tone={x.enabled?'ok':'pending'}>{x.enabled?'فعال':'Disabled'}</Badge></p><p>Mode: <b>{!x.enabled?'Disabled':x.provider==='auto'?'Auto':x.provider==='mock'?'Mock only for dev':`Force ${x.provider}`}</b></p>{x.taskType!=='chat'&&(!x.enabled||x.provider==='mock')&&<p className="alert warning">این قابلیت بدون Provider واقعی فعال نیست و عملکرد واقعی وانمود نمی‌شود.</p>}<JsonEditForm path={`/api/admin/ai-routes/${x.taskType}`} fields={[{name:'enabled',label:'enabled/disabled',type:'checkbox',value:x.enabled},{name:'provider',label:'mode/provider',type:'select',value:x.provider,options:modes},{name:'model',label:'model',value:x.model||'auto'}]}/></SectionCard>)}</div>{!items.length&&<EmptyState/>}</>}
