'use client';import {useState}from'react';export default function Login(){const[err,setErr]=useState('');async function submit(e:any){e.preventDefault();const fd=new FormData(e.currentTarget);const r=await fetch('/api/admin/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:fd.get('email'),password:fd.get('password')})});if(r.ok) location.href='/dashboard'; else setErr('ورود ناموفق بود')}return <div className="login card"><h1>ورود ادمین</h1><p className="muted">پنل مدیریت مونِس AI</p><form onSubmit={submit} style={{display:'grid',gap:12}}><input name="email" placeholder="ایمیل"/><input name="password" type="password" placeholder="رمز عبور"/><button className="btn">ورود امن</button>{err&&<p>{err}</p>}</form></div>}
