'use client';import {useState}from'react';export default function Login(){const[err,setErr]=useState('');const[loading,setLoading]=useState(false);async function submit(e:any){e.preventDefault();setErr('');setLoading(true);const fd=new FormData(e.currentTarget);const r=await fetch('/api/admin/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:fd.get('email'),password:fd.get('password')})});setLoading(false);if(r.ok) location.href='/dashboard'; else setErr('ایمیل یا رمز عبور درست نیست.')}return <main className="loginPage"><section className="login card"><div className="logo">اوستا</div><h1>ورود به پنل مدیریت</h1><p className="muted">پنل مدیریت ربات هوش مصنوعی</p><form onSubmit={submit} className="form"><input name="email" type="email" placeholder="ایمیل مدیر" required/><input name="password" type="password" placeholder="رمز عبور" required/><button className="btn" disabled={loading}>{loading?'در حال ورود...':'ورود امن'}</button>{err&&<p className="error">{err}</p>}</form></section></main>}
