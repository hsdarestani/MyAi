import { api } from '../proxy';import { PageHeader,SectionCard,Table,Money,DateTime,Badge,EmptyState } from '../components/ui';
export default async function Page(){const d=await api('/api/admin/payments');const items=d.items||[];return <><PageHeader title="پرداخت‌ها" desc="کنسول عملیاتی اوستا"/>{d.error&&<SectionCard><p className="error">{d.message}</p></SectionCard>}<SectionCard><Table headers={["کاربر","مبلغ","Provider","وضعیت","تاریخ","عملیات"]}>{items.map((x:any)=><tr key={x.id}><td><a href={`/users/${x.userId}`}>{x.user?.username||x.user?.telegramId}</a></td><td><Money value={x.amount}/></td><td>{x.provider}</td><td><Badge tone={x.status}>{x.status}</Badge></td><td><DateTime value={x.createdAt}/></td><td className="actions"><button className="btn">تأیید</button><button className="ghost">رد</button></td></tr>)}</Table>{!items.length&&<EmptyState text="پرداخت در انتظاری وجود ندارد"/>}</SectionCard></>}
