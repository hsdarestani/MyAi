import { api } from '../../proxy';const fmt=(n:any)=>new Intl.NumberFormat('fa-IR').format(Number(n||0));export default async function User({params}:{params:{id:string}}){const d=await api(`/api/admin/users/${params.id}`);return <><h1>جزئیات کاربر</h1><div className="grid"><div className="card"><h3>{d.user?.firstName||'کاربر'}</h3><p>موجودی: {fmt(d.user?.walletBalance)} تومان</p><p>وضعیت: {d.user?.status}</p></div><div className="card"><h3>تنظیم دستی کیف پول</h3><p className="muted">از API امن ادمین برای افزایش/کاهش موجودی استفاده می‌شود.</p></div></div></>}
