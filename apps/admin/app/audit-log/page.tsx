import { api } from '../proxy';import { PageHeader,SectionCard,Table,Money,DateTime,Badge,EmptyState } from '../components/ui';
export default async function Page(){const d=await api('/api/admin/audit-log');const items=d.items||[];return <><PageHeader title="لاگ فعالیت" desc="کنسول عملیاتی اوستا"/>{d.error&&<SectionCard><p className="error">{d.message}</p></SectionCard>}<SectionCard><Table headers={["عملیات","ادمین","Entity","تاریخ"]}>{items.map((x:any)=><tr key={x.id}><td>{x.action}</td><td>{x.admin?.email}</td><td>{x.entityType} {x.entityId}</td><td><DateTime value={x.createdAt}/></td></tr>)}</Table>{!items.length&&<EmptyState/>}</SectionCard></>}
