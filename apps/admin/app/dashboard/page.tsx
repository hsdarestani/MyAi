import { api } from '../proxy';import { PageHeader,StatCard,SectionCard,Table,Money,DateTime,Badge,EmptyState } from '../components/ui';
const labels:any={users:'کل کاربران',newUsers:'کاربران جدید امروز',active:'کاربران فعال امروز',totalWallet:'موجودی کل کیف پول‌ها',revenueToday:'درآمد امروز',providerCostToday:'هزینه Provider امروز',grossMargin:'سود ناخالص امروز',chat:'تعداد چت‌ها',image:'تعداد ساخت عکس',video:'تعداد ویدیوها',pendingPayments:'پرداخت‌های در انتظار',failedJobs:'جاب‌های ناموفق',pendingJobs:'جاب‌های در صف'};
export default async function Page(){const d=await api('/api/admin/dashboard');return <><PageHeader title="داشبورد" desc="نمای سریع سلامت کسب‌وکار، مصرف و کیف پول‌ها"/>{d.error&&<SectionCard><p className="error">{d.message}</p></SectionCard>}<div className="grid">{Object.entries(d.cards||{}).map(([k,v]:any)=><StatCard key={k} label={labels[k]||k} value={v}/>)}</div><div className="grid"><SectionCard title="هشدارها"><p><Badge tone={d.alerts?.providerConfigured?'ok':'failed'}>{d.alerts?.providerConfigured?'Provider تنظیم شده':'Provider تنظیم نشده'}</Badge></p><p>عکس: <Badge tone={d.alerts?.imageEnabled?'ok':'pending'}>{d.alerts?.imageEnabled?'فعال':'غیرفعال تا اتصال provider واقعی'}</Badge></p><p>ویدیو: <Badge tone={d.alerts?.videoEnabled?'ok':'pending'}>{d.alerts?.videoEnabled?'فعال':'غیرفعال تا اتصال provider واقعی'}</Badge></p><p>کاربران کم‌موجودی: {d.alerts?.lowWallet??0}</p></SectionCard><SectionCard title="روند مصرف"><p className="muted">نمودار تفصیلی در نسخه بعدی اضافه می‌شود؛ جدول‌های اخیر پایین وضعیت عملیاتی را نشان می‌دهند.</p></SectionCard></div><SectionCard title="آخرین مصرف AI"><Table headers={['کاربر','نوع','Provider','هزینه','وضعیت','تاریخ']}>{(d.recentUsage||[]).map((x:any)=><tr key={x.id}><td>{x.user?.username||x.user?.telegramId}</td><td>{x.taskType}</td><td>{x.internalProvider}</td><td><Money value={x.finalUserCharge}/></td><td><Badge tone={x.status}>{x.status}</Badge></td><td><DateTime value={x.createdAt}/></td></tr>)}</Table>{!d.recentUsage?.length&&<EmptyState/>}</SectionCard><SectionCard title="آخرین تراکنش‌های کیف پول"><Table headers={['کاربر','نوع','مبلغ','مانده بعد','شرح','تاریخ']}>{(d.recentLedger||[]).map((x:any)=><tr key={x.id}><td>{x.user?.username||x.user?.telegramId}</td><td>{x.type}</td><td className={x.amount>=0?'amountPos':'amountNeg'}><Money value={x.amount}/></td><td><Money value={x.balanceAfter}/></td><td>{x.description}</td><td><DateTime value={x.createdAt}/></td></tr>)}</Table>{!d.recentLedger?.length&&<EmptyState/>}</SectionCard></>}
