export const fmt=(n:any)=>new Intl.NumberFormat('fa-IR').format(Number(n||0));export function Money({value}:{value:any}){return <span>{fmt(value)} تومان</span>}export function DateTime({value}:{value:any}){return <span>{value?new Date(value).toLocaleString('fa-IR'):''}</span>}export function Badge({children,tone='neutral'}:{children:any;tone?:string}){return <span className={`badge ${tone}`}>{children}</span>}export function PageHeader({title,desc}:{title:string;desc?:string}){return <div className="top"><div><h1>{title}</h1>{desc&&<p className="muted">{desc}</p>}</div><span className="muted">ai.moones.top</span></div>}export function SectionCard({title,children}:{title?:string;children:any}){return <section className="card">{title&&<h2>{title}</h2>}{children}</section>}export function EmptyState({text='هنوز داده‌ای ثبت نشده'}:{text?:string}){return <div className="empty">{text}</div>}export function StatCard({label,value}:{label:string;value:any}){return <div className="card stat"><span>{label}</span><strong>{typeof value==='number'?fmt(value):value}</strong></div>}export function Table({headers,children}:{headers:string[];children:any}){return <div className="tableWrap"><table className="table"><thead><tr>{headers.map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{children}</tbody></table></div>}export function DangerButton({children}:{children:any}){return <button className="danger">{children}</button>}
