import { api } from '../proxy';import { PageHeader,SectionCard,Table,Money,DateTime,Badge,EmptyState } from '../components/ui';
export default async function Page(){const d=await api('/api/admin/me');const items=d.items||[];return <><PageHeader title="ادمین‌ها" desc="کنسول عملیاتی اوستا"/>{d.error&&<SectionCard><p className="error">{d.message}</p></SectionCard>}<SectionCard><Table headers={["ایمیل","نقش"]}><tr><td>{d.admin?.email}</td><td><Badge tone="ok">{d.admin?.role}</Badge></td></tr></Table></SectionCard></>}
