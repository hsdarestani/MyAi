import { z } from 'zod';
export const envSchema = z.object({ NODE_ENV:z.string().default('development'), DATABASE_URL:z.string().default('postgresql://postgres:postgres@postgres:5432/moones'), REDIS_URL:z.string().default('redis://redis:6379'), JWT_SECRET:z.string().default('dev-secret-change'), ADMIN_EMAIL:z.string().email().default('admin@moones.top'), ADMIN_PASSWORD:z.string().default('ChangeMe123!'), TELEGRAM_BOT_TOKEN:z.string().optional(), AI_PROVIDER_MODE:z.enum(['mock','venice','openai-compatible']).default('mock'), VENICE_API_KEY:z.string().optional(), OPENAI_COMPATIBLE_BASE_URL:z.string().optional(), OPENAI_COMPATIBLE_API_KEY:z.string().optional(), PUBLIC_APP_URL:z.string().default('https://ai.moones.top'), WEBHOOK_SECRET:z.string().optional(), PAYMENT_PROVIDER:z.enum(['mock','manual','zarinpal']).default('mock'), PAYMENT_CALLBACK_URL:z.string().default('https://ai.moones.top/api/payments/callback'), API_URL:z.string().default('http://api:4000') });
export const env = envSchema.parse(process.env);
